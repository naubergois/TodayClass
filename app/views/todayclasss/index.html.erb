<h1>Today Classes</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Summary</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<% @todayclasss.each do |todayclass| %>
<tr>
    <td><%= todayclass.title %></td>
    <td><%= todayclass.content %></td>
    <td><%= link_to "Show", today %></td>
    <td><%= link_to "Edit", edit_book_path(todayclass) %></td>
    <td><%= link_to "Remove", todayclass, method: :delete, data: { confirm: "Are you sure?" } %></td>
  </tr>
<% end %>
</table>
 
<br>

